---
permalink: /features/
layout: base.njk
pageTitle: Features
pageDescription: "Explore every feature of Mermaid Toolkit for Google Docs — from one-click rendering to Fix Copied Markdown."
---
  <main>
  <section class="page-hero">
    <h1>Features</h1>
    <p>Everything the add-on can do — and why each feature exists.</p>
  </section>

  <section class="features-detail">
    <div class="features-inner">

      <!-- CONVERT ALL -->
      <div class="feature-block reveal" id="convert-all">
        <div class="feature-text">
          <div class="feature-tag">Core</div>
          <h2>Convert All Code to Diagrams</h2>
          <p>Scans your entire Google Doc for Mermaid code blocks — both native <code>Code Snippet</code> elements and fenced <code>```mermaid</code> blocks — and renders every one as a high-quality PNG image. You preview each diagram before choosing to <strong>Insert</strong> (after the code) or <strong>Replace</strong> (swap the code block).</p>
          <p class="feature-menu"><strong>Extensions → Mermaid Toolkit → Convert All Code to Diagrams</strong></p>
        </div>
        <div class="feature-media">
          <svg viewBox="0 0 480 300" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Convert All screenshot placeholder">
            <rect width="480" height="300" rx="12" fill="#111818"/>
            <rect x="1" y="1" width="478" height="298" rx="11" fill="none" stroke="rgba(74,234,204,0.15)" stroke-width="1"/>
            <text x="240" y="155" text-anchor="middle" fill="rgba(74,234,204,0.5)" font-family="Space Mono,monospace" font-size="13">TODO: convert-all screenshot</text>
          </svg>
        </div>
      </div>

      <!-- CONVERT SELECTED -->
      <div class="feature-block feature-block-reverse reveal" id="convert-selected">
        <div class="feature-text">
          <div class="feature-tag">Core</div>
          <h2>Convert Selected Code to Diagram</h2>
          <p>Select a single code block or fenced Mermaid block and convert just that one. Useful when you want to render one diagram without scanning the whole document.</p>
          <p class="feature-menu"><strong>Extensions → Mermaid Toolkit → Convert Selected Code to Diagram</strong></p>
        </div>
        <div class="feature-media">
          <svg viewBox="0 0 480 300" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Convert Selected screenshot placeholder">
            <rect width="480" height="300" rx="12" fill="#111818"/>
            <rect x="1" y="1" width="478" height="298" rx="11" fill="none" stroke="rgba(74,234,204,0.15)" stroke-width="1"/>
            <text x="240" y="155" text-anchor="middle" fill="rgba(74,234,204,0.5)" font-family="Space Mono,monospace" font-size="13">TODO: convert-selected screenshot</text>
          </svg>
        </div>
      </div>

      <!-- EDITOR -->
      <div class="feature-block reveal" id="editor">
        <div class="feature-text">
          <div class="feature-tag">Editor</div>
          <h2>Insert Mermaid Diagram</h2>
          <p>Opens a full-featured side-by-side editor with live preview. Write or paste Mermaid syntax on the left, see the rendered diagram update in real time on the right. Choose from built-in templates to jump-start any diagram type.</p>
          <p class="feature-menu"><strong>Extensions → Mermaid Toolkit → Insert Mermaid Diagram</strong></p>
        </div>
        <div class="feature-media">
          <svg viewBox="0 0 480 300" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Editor screenshot placeholder">
            <rect width="480" height="300" rx="12" fill="#111818"/>
            <rect x="1" y="1" width="478" height="298" rx="11" fill="none" stroke="rgba(74,234,204,0.15)" stroke-width="1"/>
            <text x="240" y="155" text-anchor="middle" fill="rgba(74,234,204,0.5)" font-family="Space Mono,monospace" font-size="13">TODO: editor screenshot</text>
          </svg>
        </div>
      </div>

      <!-- EDIT IN PLACE -->
      <div class="feature-block feature-block-reverse reveal" id="edit-in-place">
        <div class="feature-text">
          <div class="feature-tag">Editor</div>
          <h2>Edit Selected Mermaid Image</h2>
          <p>Click on any Mermaid diagram image that was inserted by this add-on, then run this command. The editor opens pre-loaded with the diagram's source code so you can tweak and re-render without starting over.</p>
          <p class="feature-menu"><strong>Extensions → Mermaid Toolkit → Edit Selected Mermaid Image</strong></p>
        </div>
        <div class="feature-media">
          <svg viewBox="0 0 480 300" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Edit in Place screenshot placeholder">
            <rect width="480" height="300" rx="12" fill="#111818"/>
            <rect x="1" y="1" width="478" height="298" rx="11" fill="none" stroke="rgba(74,234,204,0.15)" stroke-width="1"/>
            <text x="240" y="155" text-anchor="middle" fill="rgba(74,234,204,0.5)" font-family="Space Mono,monospace" font-size="13">TODO: edit-in-place screenshot</text>
          </svg>
        </div>
      </div>

      <!-- IMAGE TO CODE -->
      <div class="feature-block reveal" id="image-to-code">
        <div class="feature-text">
          <div class="feature-tag">Reverse</div>
          <h2>Convert Image to Code</h2>
          <p>Every diagram image inserted by this add-on embeds the original Mermaid source in its alt text. Use <strong>Convert Selected Image to Code</strong> on a single image, or <strong>Convert All Images to Code</strong> to extract source from every diagram in the document.</p>
          <p class="feature-menu"><strong>Extensions → Mermaid Toolkit → Convert Selected Image to Code</strong><br><strong>Extensions → Mermaid Toolkit → Convert All Images to Code</strong></p>
        </div>
        <div class="feature-media">
          <svg viewBox="0 0 480 300" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Image to Code screenshot placeholder">
            <rect width="480" height="300" rx="12" fill="#111818"/>
            <rect x="1" y="1" width="478" height="298" rx="11" fill="none" stroke="rgba(74,234,204,0.15)" stroke-width="1"/>
            <text x="240" y="155" text-anchor="middle" fill="rgba(74,234,204,0.5)" font-family="Space Mono,monospace" font-size="13">TODO: image-to-code screenshot</text>
          </svg>
        </div>
      </div>

      <!-- FIX MARKDOWN — THE BIG STORY -->
      <div class="feature-block-highlight reveal" id="fix-markdown">
        <div class="feature-highlight-header">
          <div class="feature-tag">Utility</div>
          <h2>Fix Copied Markdown</h2>
          <p class="feature-highlight-lead">The one feature you didn't know you needed — until Google Docs breaks your diagrams.</p>
        </div>

        <div class="problem-story">
          <h3>The Code Snippet Problem</h3>
          <p>Google Docs has a built-in <strong>Code Snippet</strong> block (Insert → Code block). It looks great for displaying code, but it has a critical flaw: when you copy text from a markdown file, a README, or any external source and paste it into a Code Snippet, Google Docs silently corrupts the content.</p>
          <div class="problem-list">
            <div class="problem-item">
              <div class="problem-icon">⚡</div>
              <div>
                <strong>Vertical tab characters (<code>\v</code>)</strong>
                <p>Newlines inside pasted code blocks are replaced with vertical tab characters (<code>U+000B</code>). These are invisible but break Mermaid parsing completely.</p>
              </div>
            </div>
            <div class="problem-item">
              <div class="problem-icon">🔤</div>
              <div>
                <strong>Stray backticks</strong>
                <p>Fenced code block markers (<code>```mermaid</code>) survive the paste and wrap lines that shouldn't have them, producing invalid syntax.</p>
              </div>
            </div>
            <div class="problem-item">
              <div class="problem-icon">📋</div>
              <div>
                <strong>Formatting artifacts</strong>
                <p>Rich-text formatting from the source (bold, italic, links) can inject invisible Unicode characters or extra whitespace that Mermaid doesn't expect.</p>
              </div>
            </div>
          </div>
        </div>

        <div class="solution-story">
          <h3>How Fix Copied Markdown works</h3>
          <p>Paste your corrupted code into the tool. It automatically:</p>
          <ol>
            <li>Replaces all <code>\v</code> (vertical tab) characters with proper newlines</li>
            <li>Strips stray backtick fences that wrapped individual lines</li>
            <li>Removes invisible formatting artifacts</li>
            <li>Shows a side-by-side diff so you can see exactly what changed</li>
          </ol>
          <p>Copy the fixed result and paste it back into your doc — it's now valid Mermaid syntax, ready to render.</p>
          <p class="feature-menu"><strong>Extensions → Mermaid Toolkit → Fix Copied Markdown</strong></p>
        </div>
      </div>

    </div>
  </section>

  <!-- PRIVACY -->
  <section class="features-privacy reveal">
    <div class="features-privacy-inner">
      <h2>🔒 Privacy by design</h2>
      <p>Zero data collection. Zero cookies. Zero telemetry. Zero servers. Your documents never leave your browser — rendering is 100% client-side via <a href="https://mermaid.js.org/">mermaid.js</a>. The only external request is loading the library from <a href="https://www.jsdelivr.com/">jsDelivr</a>.</p>
      <a href="/privacy" class="btn-dim">Read Privacy Policy →</a>
    </div>
  </section>
  </main>
